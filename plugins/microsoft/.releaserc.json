{
  "extends": "semantic-release-monorepo",
  "branches": ["main"],
  "tagFormat": "microsoft-v${version}",
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    ["@semantic-release/exec", {
      "prepareCmd": "cargo build --release --target wasm32-unknown-unknown -p microsoft-plugin && jq '.version = \"${nextRelease.version}\"' manifest.json > manifest.tmp && mv manifest.tmp manifest.json"
    }],
    ["@semantic-release/github", {
      "assets": [
        {"path": "../../target/wasm32-unknown-unknown/release/microsoft_plugin.wasm", "name": "microsoft.wasm", "label": "Microsoft Plugin WASM"},
        {"path": "manifest.json", "name": "manifest.json", "label": "Plugin Manifest"}
      ]
    }]
  ]
}
